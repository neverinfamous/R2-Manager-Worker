## [Unreleased]

### Changed
- **Dependency Updates:** Bumped package minor and patch versions via npm update, including `react` (19.2.6), `vite` (8.0.13), `wrangler` (4.91.0), and updated transitive Dockerfile patches for `tar` (7.5.15) and `brace-expansion` (5.0.6).
