---
product: "R2 Manager Worker"
version: "3.5.5"
date: "2026-08-17"
slug: "r2-manager-worker-v3-5-5"
npm_package: "r2"
docker_image: "ghcr.io/neverinfamous/r2-manager-worker"
github_repo: "neverinfamous/R2-Manager-Worker"
description: "Resolve Webhook accessibility issues, migrate to pnpm, and pin GitHub Actions by SHA."
---

# R2 Manager Worker v3.5.5

## Highlights

- **Enhanced Accessibility**: Improved Webhook configuration dialogs to fully comply with W3C standards, ensuring a seamless experience for screen readers and keyboard navigation.
- **Robust Supply Chain Security**: Migrated the package manager from `npm` to `pnpm` for faster, deterministic builds, and pinned all GitHub Actions to their exact SHA digests to prevent unauthorized pipeline tampering.

## Changed

- **deps**: Converted project from `npm` to `pnpm` for stricter dependency resolution.
- **deps**: Pinned GitHub Actions in `.github/workflows/` to exact SHA digests to secure CI/CD pipelines.

## Fixed

- **ui**: Resolved `<label>` accessibility issues in Webhook Add and Edit dialogs by implementing semantic `<fieldset>` and `<legend>` grouping.
