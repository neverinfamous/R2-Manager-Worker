## Highlights

- **Ensure Unbreakable Security**: Eliminated vulnerabilities with critical updates to transitive Docker dependencies.
- **Turbocharge Performance**: Upgraded React and Vite core packages to deliver a lightning-fast bucket management experience.

## Changed

- **Dependency Updates:** Bumped package minor and patch versions via npm update, including `react` (19.2.6), `vite` (8.0.13), `wrangler` (4.91.0), updated the transitive Dockerfile patch for `tar` (7.5.15), and updated the `package.json` override for `brace-expansion` (5.0.6).
